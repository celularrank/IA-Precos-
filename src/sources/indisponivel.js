export async function buscarAmazon() {
  // Será conectado posteriormente.
  // Nunca inventar preços.
  return [];
}

export async function buscarShopee() {
  // Será conectado posteriormente.
  // Nunca inventar preços.
  return [];
}

export async function buscarLojasOficiais() {
  // Será conectado posteriormente.
  // Nunca inventar preços.
  return [];
}
